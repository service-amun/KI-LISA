# Changelog

All notable changes to <PROJECT_NAME> are documented here.
Versions follow [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

<!-- Keep [Unreleased] at the top at all times. Move its content to a versioned
     section when cutting a release. Start a fresh [Unreleased] after each release. -->

## [Unreleased]

<!-- Add entries here as work is merged. Use the category sections below.
     Omit any category that has no entries for a given release.
     Category order: Security → Added → Changed → Deprecated → Removed → Fixed -->

## [<VERSION>] - <YYYY-MM-DD>

<!-- Replace <VERSION> and <YYYY-MM-DD>. Delete this block if no releases exist yet. -->

### Added
- 

### Fixed
- 

<!-- Version diff links — OPTIONAL and tag-dependent. Add these only once you tag or
     release a version; the compare/<tag> and releases/tag/<tag> URLs 404 until that tag
     is pushed. A repo with no tagged releases yet ships no diff links. When you do tag,
     uncomment and keep one line per released version, newest first, updating the
     [Unreleased] base each release. -->
<!-- [Unreleased]: https://github.com/<USER>/<REPO>/compare/v<VERSION>...HEAD -->
<!-- [<VERSION>]: https://github.com/<USER>/<REPO>/releases/tag/v<VERSION> -->
